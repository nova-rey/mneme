# MNEME P2.3 Final Episodic Recurrence Preflight — 2026-09-26

Status: `READY_FOR_LIVE_EXECUTION`

This receipt freezes the final bounded Phase Two mechanism experiment authorized
in the current goal. Historical P2.3 runs, supplements, failed calls, and the
`COMPLETED_INADEQUATE` disposition remain immutable.

## Scientific contract

The experiment compares two matched local Gemma branches from clean empty
lineages. Branch `external` receives independently declared participant
circumstances that reintroduce one practical relationship after topic pivots.
Branch `model` receives the same arc coordinates but no participant
reintroduction circumstance; any recurrence must therefore be model-origin and
is recorded under the existing refractory/dependence rules.

The run uses the local Gemma 4 E4B checkpoint, local GLiNER2.5 specialist,
Qwen/DeepInfra participant and assessor roles, Schema 11 arcs, the current
semantic/provenance resolver, and the fixed learner. No coefficient,
consolidation-gap, route, provenance, or acceptance rule changes are included.

## Frozen schedule arithmetic

Each branch has 30 one-turn declared arcs. Target coordinates are
`0, 2, 11, 20, 29`; all other coordinates are unrelated departure windows.
Target coordinates therefore become learner opportunities `1, 3, 12, 21, 30`.
The first model re-entry coordinate is one round after the initial target and
is refractory for the model branch. Later target windows are eight rounds
apart. This spacing is required by the existing eight-opportunity rolling cap.
The fifth external opportunity is four relevant opportunities after the first,
so the fixed learner can perform its existing consolidation transition if all
five source-grounded observations are admitted. Same-arc repetition cannot earn
credit.

The harness plans 100 provider reservations as a hard run bound. Local Gemma
and GLiNER calls are included in the same durable ledger. The prior factual
ledger (456 attempted/returned, 455 persisted) is preserved. The current user
authorization treats this small, attributable final mechanism run as approved
Phase Two completion work; no calls are consumed by this preflight receipt.

## Integrity gates

* exact Gemma/Qwen perspective renderers are exercised by the harness;
* empty Qwen or Gemma content stops the coordinate before downstream work;
* each accepted developmental episode is persisted once;
* arc IDs, prior-arc links, initiators, refractory state, dependence, credit,
  before/after learner values, and consolidation disposition are published;
* local host fingerprints and specialist configuration are recorded;
* no historical artifact is rewritten or relabeled.

The queue dependency `review:p2.3-pilot-adequacy-after-normalization` is
resolved by this authoritative final bounded schedule and its explicit
preservation of the historical budget and scientific disposition. The run
itself remains pending until its terminal report and validation are published.
