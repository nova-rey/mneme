# MNEME Phase Two offline audit and Interloper readiness

**Status:** READY FOR EXPLICIT LIVE AUTHORIZATION

This receipt records the provider-free checkpoint prepared before the next
contingent-conversation experiment. Historical conversations, failed calls,
P2.3 qualification results, and the approved Phase Two contracts remain
unchanged.

## Interloper correction

The prospective Interloper path uses contract revision 2, policy revision 3,
`interloper-executive-v1`, and `observable-attractor-v1`. Every request receives
fresh private state containing the current environment window, approximate
remaining turns, three practical concerns, transition proximity, attractor
risk, and a compact factual summary. Only the latest two complete dialogue
pairs are provider-visible. The detector uses observable lexical repetition,
marker recurrence, short-range overlap, and late-window low-novelty signals;
it emits only `low`, `elevated`, or `high` and never selects dialogue. The
policy permits disagreement, interruption, abandoned threads, and concrete
topic changes. No learner state, desired association, evaluation criterion,
or hidden controller state is exposed to the Interloper.

The preserved Lentil Incident (`I am here.` / `We stay.` / `Enough.` /
`We remain.`) is covered by a FakeHost regression. It verifies the bounded
recent context, independent balcony-plant concerns, and elevated/high risk
signal without prescribing an exact next message.

## Shared Phase Two audit corrections in this checkpoint

- Learner admission now rejects ineligible observations and impossible
  source-role/dependence combinations without discarding their audit rows.
- Pilot artifact verification authenticates mutable state and reservation
  receipts, including their content digests.
- Frozen pilot evaluation rejects an independent valid checkpoint, authority
  drift, revoked recall, revoked provider reuse, and a mismatched bound host
  before provider dispatch.
- Production assessment publishes all bounded residue edges with complete
  monitors, preserves exposure/consequence records, and restart reuses an
  accepted developmental operation without redispatching its provider call.

These are offline integrity corrections. They do not alter learner
coefficients, historical evidence, or scientific acceptance criteria.

## Validation

- Focused Interloper tests: **16 passed**.
- Full pytest: **414 passed**.
- Ruff: passed.
- Strict mypy (`mypy src/mneme`): passed.
- Wheel build and fresh-install package smoke: passed.
- Queue JSON validation: passed.
- GitHub CI: **36080847932 passed** for `088b22876137b0d6e914f7febae844345829b6e0`.
- Provider calls for this checkpoint: **none**.
- Historical conversations were not regenerated.

The next live contingent-conversation experiment remains authorization-gated.
Transcript publication remains mandatory at every stop, continuation,
checkpoint, and completion boundary.
