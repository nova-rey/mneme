# MNEME P2.3 Matched-Twin Route-View Correction — 2026-09-25

The earlier matched-twin preflight found that repeated interpretation
materialization produced collision-safe variants that consumed the approved
`max_routes=8` route bound before query coverage was applied. The derived
specialist state therefore contained a credited garden → paint-project edge
that could not reach readout.

The bounded correction is read-only and controller-local. It groups graph-edge
variants through the authoritative semantic binding for the pinned snapshot,
retains all distinct source evidence, annotates merged source keys, and then
runs the existing deterministic route discovery and learner selection bounds.
Raw graph rows, learner values, provenance, and historical evidence are not
mutated.

The corrected audit uses the same four frozen novel probes and matched M/C
request settings. M selects the garden → paint-project route for the first
two garden/paint probes; C selects none. Moisture-only and unrelated probes
select none for both. This establishes a meaningful normal-path treatment
condition for the local matched readout. No provider or local model calls were
made by this correction.

The prior blocked preflight remains preserved at
[the original receipt](MNEME_P2.3_Matched_Twin_AB_Preflight_20260925.md).
Machine-readable corrected request/selection evidence is in [the JSON
receipt](MNEME_P2.3_Matched_Twin_AB_Route_View_Correction_20260925.json).
